// Re-exported from the Zod schema so the OpenAPI contract and TypeScript
// shape stay aligned. Edit src/shared/schemas/auth.ts to change the shape.
export type { User } from '@/shared/schemas/auth'
